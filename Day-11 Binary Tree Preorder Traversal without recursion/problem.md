# Binary Tree Preorder Traversal (Iterative)

## 📌 Problem Statement

Given the root of a binary tree, return the **preorder traversal** of its nodes’ values.

Preorder traversal follows this order:


You must solve the problem **iteratively using a stack**, not recursion.

## 🧩 Input

- `root`: The root node of a binary tree.

### TreeNode Definition
```java
class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;
}
