# Reverse Integer

## Problem Statement

Given a 32-bit signed integer `x`, reverse the digits of the integer.

If reversing `x` causes the value to go outside the signed 32-bit integer range  
`[-2³¹, 2³¹ - 1]`, return `0`.


